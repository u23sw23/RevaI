spring.application.name=demo2

# 数据库配�?
spring.datasource.url=jdbc:mysql://localhost:3306/demo2?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=985915
# Spring Boot 3.x 可以自动检测驱动，但显式指定更安全
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 数据库初始化配置（项目启动时自动执行SQL脚本�?
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql
spring.sql.init.encoding=utf-8
# 如果表已存在，是否继续执行（true=继续，false=跳过�?
spring.sql.init.continue-on-error=true
# 执行迁移脚本（添加新字段�?
spring.sql.init.additional-locations=classpath:migrate_add_options_to_questions.sql

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.example.demo.entity
mybatis.configuration.map-underscore-to-camel-case=true

# 服务器端�?
server.port=8080

# 文件上传配置
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# HTTP 超时配置（AI 生成可能需要较长时间）
server.tomcat.connection-timeout=300000
spring.mvc.async.request-timeout=300000

# 静态资源访问路径（头像存储目录�?
spring.web.resources.static-locations=classpath:/static/,file:./uploads/

# DeepSeek API Key 配置说明�?
# 方式1：直接在下面设置（不推荐，会暴露在代码中�?
# deepseek.api.key=your-api-key-here
# 方式2：使用环境变量（推荐�?
# 在系统环境变量中设置 DEEPSEEK_API_KEY，或启动时使用：
# java -jar app.jar --deepseek.api.key=your-api-key-here
# 方式3：在 IDE 运行配置中添加环境变�?DEEPSEEK_API_KEY
# deepseek.api.key=请通过环境变量 DEEPSEEK_API_KEY 配置
