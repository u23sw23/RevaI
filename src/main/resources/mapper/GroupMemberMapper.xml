<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.GroupMemberMapper">
    
    <resultMap id="GroupMemberResultMap" type="com.example.demo.entity.GroupMember">
        <id property="id" column="id"/>
        <result property="groupId" column="group_id"/>
        <result property="userId" column="user_id"/>
        <result property="joinTime" column="join_time"/>
    </resultMap>
    
    <select id="findUserIdsByGroupId" resultType="java.lang.Long">
        SELECT user_id FROM group_members WHERE group_id = #{groupId}
    </select>
    
    <select id="findGroupIdsByUserId" resultType="java.lang.Long">
        SELECT group_id FROM group_members WHERE user_id = #{userId}
    </select>
    
    <select id="findByGroupIdAndUserId" resultMap="GroupMemberResultMap">
        SELECT * FROM group_members 
        WHERE group_id = #{groupId} AND user_id = #{userId}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO group_members (group_id, user_id, join_time)
        VALUES (#{groupId}, #{userId}, #{joinTime})
    </insert>
    
    <delete id="delete">
        DELETE FROM group_members 
        WHERE group_id = #{groupId} AND user_id = #{userId}
    </delete>
    
    <delete id="deleteByGroupId">
        DELETE FROM group_members WHERE group_id = #{groupId}
    </delete>
</mapper>

